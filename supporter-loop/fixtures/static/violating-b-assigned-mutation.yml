name: static fixture
on: workflow_call
jobs:
  decide:
    steps:
      - run: |
          QUERY='mutation { updateDiscussion(input:{discussionId:1}) { discussion { id } } }'
          gh api graphql -f query="$QUERY"
