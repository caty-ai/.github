name: static fixture
on: workflow_call
jobs:
  decide:
    steps:
      - run: |
          gh api graphql -f query='mutation {
            updateDiscussion(input:{discussionId:1}) { discussion { id } }
          }'
