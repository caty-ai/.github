jobs:
  decide:
    steps:
      - run: |
          fetch_api() {
            local key=value
            set --
            set -- "$@" -f "query=@q.graphql"
            gh api graphql -f query='query { viewer { id } }' "$@"
          }
