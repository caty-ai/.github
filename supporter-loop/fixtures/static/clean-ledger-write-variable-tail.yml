name: static fixture
on: workflow_call
jobs:
  decide:
    steps:
      - run: |
          curl -X PUT https://api.github.com/repos/o/r/contents/ledger/"$name".ndjson
          gh api -X DELETE repos/o/r/contents/ledger/"$archive"
