jobs:
  decide:
    steps:
      - run: |
          fetch_api() {
            local key=value
            set --
            set -- "$@" -f "a=b"
            set -- "$@" -f "query=mutation { closeIssue }"
            gh api graphql -f query='query { viewer { id } }' "$@"
          }
